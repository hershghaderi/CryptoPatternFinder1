name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Extract Project ZIP
      run: |
        echo "Unzipping project archive..."
        unzip -o *.zip -d extracted_project
        TARGET_DIR=$(find extracted_project -name "build.gradle.kts" -o -name "build.gradle" | head -n 1 | xargs dirname)
        echo "Project directory found at: $TARGET_DIR"
        echo "PROJECT_PATH=$TARGET_DIR" >> $GITHUB_ENV
        cd $TARGET_DIR
        gradle wrapper

    - name: Build Debug APK
      run: |
        cd ${{ env.PROJECT_PATH }}
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon

    - name: Rename APK to Hersh
      run: |
        cd ${{ env.PROJECT_PATH }}/app/build/outputs/apk/debug/
        mv app-debug.apk Hersh-CryptoPatternFinder.apk

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Hersh-CryptoPatternFinder
        path: ${{ env.PROJECT_PATH }}/app/build/outputs/apk/debug/Hersh-CryptoPatternFinder.apk
